<section class="trust section">
  <div class="container">
    <div class="logo-row animate-on-scroll">
      <span>Trusted by industry leaders and integrated with</span>
      {['Tally', 'SAP', 'Zoho', 'Salesforce'].map((logo) => <strong>{logo}</strong>)}
    </div>
    <div class="stats grid four animate-on-scroll stagger-children">
      <div><strong>15+</strong><span>Projects Delivered</span></div>
      <div><strong>6</strong><span>Industries Served</span></div>
      <div><strong>93%</strong><span>Client Retention Rate</span></div>
      <div><strong>2 weeks</strong><span>Discovery to Kickoff</span></div>
    </div>
    <figure class="quote animate-on-scroll">
      <blockquote>Kunal Karnik did not just build us an app. He showed us where we were losing time and money, then helped our supervisors adopt the system in days.</blockquote>
      <figcaption>Operations Head, Manufacturing Company</figcaption>
    </figure>
  </div>
</section>

<style>
  .trust {
    padding: 46px 0 72px;
  }

  .logo-row {
    display: flex;
    flex-wrap: wrap;
    gap: 14px;
    align-items: center;
    justify-content: center;
    color: var(--muted);
  }

  .logo-row strong {
    min-width: 98px;
    border: 1px solid var(--line);
    border-radius: var(--radius);
    padding: 10px 16px;
    text-align: center;
    color: #475569;
    filter: grayscale(1);
    background: #fff;
  }

  .stats {
    grid-template-columns: repeat(4, minmax(0, 1fr));
    margin-top: 32px;
    text-align: center;
  }

  .stats div {
    border-right: 1px solid var(--line);
  }

  .stats div:last-child {
    border-right: 0;
  }

  .stats strong,
  .stats span {
    display: block;
  }

  .stats strong {
    color: var(--blue);
    font-size: clamp(1.7rem, 3vw, 2.4rem);
    line-height: 1.1;
  }

  .stats span {
    color: var(--muted);
    font-weight: 700;
  }

  .quote {
    max-width: 850px;
    margin: 36px auto 0;
    border-left: 4px solid var(--green);
    padding-left: 24px;
  }

  blockquote {
    margin: 0;
    color: var(--charcoal);
    font-size: 1.2rem;
    font-weight: 750;
  }

  figcaption {
    margin-top: 10px;
    color: var(--muted);
  }

  @media (max-width: 760px) {
    .stats {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      row-gap: 24px;
    }

    .stats div {
      border-right: 0;
    }
  }
</style>
