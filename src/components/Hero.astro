<section class="hero">
  <div class="container hero-grid">
    <div class="hero-copy">
      <span class="eyebrow">Technology strategy and delivery</span>
      <h1>Your industry expertise. My technology. Together, we solve what slows you down.</h1>
      <p class="lead">With nearly a decade across engineering, technical leadership, product delivery, and team management, I help organizations turn operational friction into reliable digital products with measurable business value.</p>
      <div class="hero-actions">
        <a class="button primary" href="#contact">Let's Talk</a>
        <a class="button secondary" href="/portfolio">See Featured Projects</a>
      </div>
    </div>
    <div class="hero-visual" aria-hidden="true">
      <img src="/images/og/kunal-operations-hero.png" alt="" />
      <div class="visual-chip top">
        <strong>15+</strong>
        <span>Projects delivered</span>
      </div>
      <div class="visual-chip bottom">
        <strong>6</strong>
        <span>Industries served</span>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: calc(100vh - 72px);
    display: grid;
    align-items: center;
    padding: 116px 0 64px;
    background: linear-gradient(135deg, #eff6ff 0%, #fff 48%, #ecfdf5 100%);
    overflow: hidden;
  }

  .hero .container {
    width: min(1360px, calc(100% - 56px));
  }

  .hero-grid {
    display: grid;
    grid-template-columns: minmax(440px, 0.88fr) minmax(520px, 1.12fr);
    gap: clamp(42px, 5vw, 86px);
    align-items: center;
  }

  .hero-copy {
    animation: fadeInUp 0.7s ease both;
  }

  .hero h1 {
    margin-top: 18px;
    max-width: 740px;
    font-size: clamp(3rem, 4.2vw, 5rem);
    line-height: 1.02;
  }

  .hero p {
    max-width: 660px;
    margin-top: 24px;
    font-size: clamp(1.04rem, 1.25vw, 1.22rem);
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 14px;
    margin-top: 32px;
    animation: fadeInUp 0.7s ease 0.16s both;
  }

  .hero-visual {
    position: relative;
    border: 1px solid rgba(30, 64, 175, 0.16);
    border-radius: 8px;
    overflow: hidden;
    box-shadow: 0 28px 70px rgba(30, 41, 59, 0.18);
    animation: scaleIn 0.8s ease 0.18s both;
    isolation: isolate;
  }

  .hero-visual img {
    width: 100%;
    height: clamp(430px, 42vw, 610px);
    object-fit: cover;
    object-position: center;
  }

  .visual-chip {
    position: absolute;
    z-index: 1;
    display: grid;
    gap: 2px;
    min-width: 150px;
    border: 1px solid rgba(226, 232, 240, 0.9);
    border-radius: 8px;
    padding: 13px 16px;
    background: rgba(255, 255, 255, 0.88);
    box-shadow: 0 16px 42px rgba(15, 23, 42, 0.14);
    backdrop-filter: blur(10px);
  }

  .visual-chip.top {
    top: 22px;
    left: 22px;
  }

  .visual-chip.bottom {
    right: 22px;
    bottom: 22px;
  }

  .visual-chip strong {
    color: var(--blue);
    font-size: 1.6rem;
    line-height: 1;
  }

  .visual-chip span {
    color: #475569;
    font-size: 0.82rem;
    font-weight: 800;
  }

  @media (max-width: 1120px) {
    .hero-grid {
      grid-template-columns: minmax(0, 1fr) minmax(380px, 0.9fr);
    }

    .hero h1 {
      font-size: clamp(2.7rem, 5vw, 4rem);
    }
  }

  @media (max-width: 900px) {
    .hero {
      min-height: auto;
    }

    .hero .container {
      width: min(100% - 36px, 760px);
    }

    .hero-grid {
      grid-template-columns: 1fr;
    }

    .hero-visual img {
      height: 360px;
    }
  }

  @media (max-width: 560px) {
    .hero {
      padding-top: 102px;
    }

    .hero h1 {
      font-size: clamp(2.35rem, 12vw, 3rem);
    }

    .visual-chip {
      display: none;
    }
  }
 </style>
